# ── Community Backend Configuration ──────────────────────────────────────────
# 运行模式: public(前端) | admin(管理) | both(本地开发)
APP_MODE=both

# 自身服务
APP_HOST=0.0.0.0
APP_PORT=50051

# 数据库
DATABASE_URL=postgresql+asyncpg://postgres:123456@localhost:5432/community

# CORS
COMMUNITY_CORS_ORIGINS=["http://localhost:3002","http://localhost:5174"]
